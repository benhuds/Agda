monotone
(λ x →
   0 ,
   monotone
   (λ p₁ →
      fst (natrec (1 , 0) (λ n x₂ → S (fst x₂) , S (S (snd x₂))) p₁) ,
      snd (natrec (1 , 0) (λ n x₂ → S (fst x₂) , S (S (snd x₂))) p₁))
   (λ a b c → ERASED))
(λ x y z₁ → ERASED)
